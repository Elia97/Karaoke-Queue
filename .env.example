# =============================================================================
# Karaoke Expo Client - Environment Variables
# =============================================================================

# Socket.IO server URL
# -----------------------------------------------------------------------------
# LOCAL DEVELOPMENT (browser/emulatore su stesso PC):
#   EXPO_PUBLIC_SOCKET_URL=http://localhost:3000
#
# MOBILE DEVELOPMENT (smartphone fisici su stessa rete Wi-Fi):
#   EXPO_PUBLIC_SOCKET_URL=http://YOUR_LOCAL_IP:3000
#
# Come trovare il tuo IP locale:
#   Linux:   ip addr show | grep "inet " | grep -v "127.0.0.1"
#   macOS:   ipconfig getifaddr en0
#   Windows: ipconfig (cerca IPv4 Address)
# -----------------------------------------------------------------------------
EXPO_PUBLIC_SOCKET_URL=http://localhost:3000

# PIN per accedere alla modalità conduttore
# Accedi con ?host=PIN nell'URL (es: http://localhost:8081?host=1234)
EXPO_PUBLIC_HOST_PIN=1234
